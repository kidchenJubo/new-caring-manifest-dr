apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: new-caring-web-page-release
  namespace: istio-system
spec:
  hosts:
    - "new-caring-release.jubo.health"
  gateways:
    - istio-system/infra-gateway-external-release
  http:
    - route:
        - destination:
            host: new-caring-web-page.new-caring-web-page-release.svc.cluster.local
            port:
              number: 80
