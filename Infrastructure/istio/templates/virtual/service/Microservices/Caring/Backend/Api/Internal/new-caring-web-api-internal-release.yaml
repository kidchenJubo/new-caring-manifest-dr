apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: new-caring-web-api-internal-release
  namespace: istio-system
spec:
  hosts:
    - "*.caringcm.com.tw"
  gateways:
    - istio-system/infra-gateway-internal-release
  http:
    - match:
        - uri:
            prefix: /api
      route:
        - destination:
            host: new-caring-web-api.new-caring-web-api-release.svc.cluster.local
            port:
              number: 80
