apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: new-caring-web-api-storage-demo
  namespace: istio-system
spec:
  hosts:
    - "new-caring-demo-api-storage.jubo.health"
  gateways:
    - istio-system/infra-gateway-external-demo
  http:
    - rewrite:
        authority: storage.googleapis.com
      route:
        - destination:
            host: storage.googleapis.com
            port:
              number: 443
