apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: new-caring-mobile-api-neg-dev
  namespace: istio-system
spec:
  hosts:
    - "mobile-api-dev.caringcm.com.tw"
  gateways:
    - istio-system/infra-gateway-neg-dev
  http:
    - match:
        - uri:
            prefix: /api
        - uri:
            prefix: /swagger/
      route:
        - destination:
            host: new-caring-mobile-api.new-caring-mobile-api-dev.svc.cluster.local
            port:
              number: 80