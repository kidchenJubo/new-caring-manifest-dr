apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: infra-gateway-neg
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - "*.caringcm.com.tw"
      port:
        name: http
        number: 80
        protocol: HTTP
