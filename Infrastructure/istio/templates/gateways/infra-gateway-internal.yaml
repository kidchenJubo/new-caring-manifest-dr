apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: infra-gateway-internal
spec:
  selector:
    istio: istio
  servers:
    - hosts:
        - "*.caringcm.com.tw"
        - "*" # 允許內部訪問
      port:
        name: http
        number: 80
        protocol: HTTP
