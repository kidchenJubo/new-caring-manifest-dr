{{- if .Values.Settings.Urls.QatFrontendCaring }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: new-caring-web-page-qat
  namespace: istio-system
spec:
  hosts:
    - {{ .Values.Settings.Urls.QatFrontendCaring }}
  gateways:
    - istio-system/infra-gateway
  http:
    - route:
        - destination:
            host:  new-caring-web-page.new-caring-web-page-qat.svc.cluster.local
            port:
              number: 80
{{ end }}