{{- if .Values.Settings.Urls.DevFrontendCaring }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: new-caring-web-page-dev
  namespace: istio-system
spec:
  hosts:
    - {{ .Values.Settings.Urls.DevFrontendCaring }}
  gateways:
    - istio-system/infra-gateway
  http:
    - route:
        - destination:
            host:  new-caring-web-page.new-caring-web-page-dev.svc.cluster.local
            port:
              number: 80
{{ end }}