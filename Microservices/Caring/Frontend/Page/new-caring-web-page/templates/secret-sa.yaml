{{- if .Values.App.ServiceAccountCredentials }}
  kind: Secret
apiVersion: v1
metadata:
  name: new-caring-web-page-sa-secret
data:
    service-account-credentials.json: {{ .Values.App.ServiceAccountCredentials | quote }}
{{- end }}
